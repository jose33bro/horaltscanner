# Requirements pour Horaltscanner

# Communication USB
pyserial>=3.5

# GPIO Raspberry Pi
gpiozero>=1.6.0
RPi.GPIO>=0.7.0

# Vision par ordinateur
opencv-python>=4.5.0
opencv-contrib-python>=5.0.0.93

# Calcul scientifique
# On RPi ARM64 (aarch64): numpy 2.5.2+ not available on piwheels.
# Use <= 2.1.x on ARM64, but allow 2.5.2+ on x86/amd64 for faster CI.
numpy>=2.0.0,<2.2.0; platform_machine == "aarch64" or platform_machine == "arm64"
numpy>=2.5.2; platform_machine != "aarch64" and platform_machine != "arm64"
scipy>=1.7.0

# Images
Pillow>=8.3.0

# Caméra Raspberry Pi
picamera2>=0.3.37  # optionnel

# 3D et géométrie
# PyPI does not publish Linux ARM64 wheels. Raspberry Pi uses the official
# source installer in software/scripts/install_open3d_pi.sh.
open3d==0.19.0; platform_machine != "aarch64" and platform_machine != "arm64"

# Tests
pytest>=7.0.0
pytest-cov>=3.0.0

# Logging et monitoring
python-dateutil>=2.8.0

# JSON/YAML
pyyaml>=5.4.0

# Web API
flask>=3.1.3
flask-cors>=4.0.0

# Production WSGI server (multi-worker, non-blocking async reconstruction)
gunicorn>=22.0.0
gevent>=23.9.0

# HTTP client (Moonraker)
requests>=2.31.0
