# CI/test-only dependencies (non-Raspberry Pi runners).
# Kept as the single source of truth for the package set installed by both
# .github/workflows/ci.yml and .github/workflows/copilot-setup-steps.yml.
pytest>=9.1.1
pytest-cov>=7.1.0
flask>=3.1.3
flask-cors>=6.0.0
pydantic>=2.0.0
requests>=2.34.2
numpy>=2.5.2; python_version >= "3.12" and (platform_machine == "aarch64" or platform_machine == "arm64")
numpy>=2.4.6,<2.5; python_version < "3.12" and (platform_machine == "aarch64" or platform_machine == "arm64")
numpy>=2.5.2; python_version >= "3.12" and platform_machine != "aarch64" and platform_machine != "arm64"
numpy>=2.4.6,<2.5; python_version < "3.12" and platform_machine != "aarch64" and platform_machine != "arm64"
scipy>=1.18.1
pyyaml>=5.4.0
python-dateutil>=2.9.0.post0
pyserial>=3.5
pillow>=8.3.0
opencv-python-headless>=4.5.0
